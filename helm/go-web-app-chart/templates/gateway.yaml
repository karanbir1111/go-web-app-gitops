apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: go-project-gateway
  namespace: default
spec:
  gatewayClassName: envoy-gateway-class
  listeners:
    - name: http-listener
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same
